<h1 class="main-title">After Midnight</h1>


<h2>Find something that's open</h2>

<div class="form-box">
<%= form_for(@result) do |f| %>
  <% if @result.errors.any? %>
    <div id="error_explanation" class="alert alert-danger" "alert-dismissable" >
      <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      <h4><%= pluralize(@result.errors.count, "error") %> prohibited this result from being saved:</h4>

      <ul>
      <% @result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :street %><br>
    <%= f.text_field :street, class: "form-control", placeholder: "1000 Steel Street" %>
  </div>

  <div class="form-group">
    <%= f.label :city %><br>
    <%= f.text_field :city, class: "form-control",
    placeholder: "Saint Louis" %>
  </div>

  <div class="form-group">
    <%= f.label :state %><br>
    <%= f.text_field :state, class: "form-control", placeholder: "MO" %>
  </div>

  <div class="form-group">
    <%= f.label :zip %><br>
    <%= f.text_field :zip, class="form-control", placeholder: "90210" %>
  </div>
  
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
</div>